<h1>ブログ一覧</h1>
<p><%= notice %></p>

<table>
  <tr>
    <th>tweeter</th>
    <th>内容</tr>
   </tr>
 <% @posts.each do |post| %>
    <tr>
      
       <td><%= post.content %></td>
       <td><%= link_to '詳細を確認する', post_path(post.id) %></td>
      

    </tr>
 <% end %>
</table>
<br>
<%= link_to '新しく投稿を作成する', new_post_path %>